# 미션 진행 가이드: 개발자 워크스테이션 구축 (Linux CLI + Docker + Git/GitHub)

> 이 문서는 `docs/reference.md`에 정의된 미션 요구사항을 직접 해결하기 위한 **개인용 진행 가이드 겸 체크리스트**입니다.
> 실제 제출용 "기술 문서"는 [`README.md`](./README.md)입니다 — 각 섹션 완료 시 두 문서를 함께 체크하세요.

## 사용 방법

1. 아래 체크리스트를 위에서부터 순서대로 진행하세요. (터미널 → 권한 → Docker 설치 → Docker 운영 → 컨테이너 → 커스텀 이미지 → 포트/볼륨 → Git 순으로 난이도가 쌓이도록 배치했습니다.)
2. 각 항목을 완료하면 `- [ ]`를 `- [x]`로 바꾸세요.
3. "접근 힌트"는 개념/점검 관점만 제시합니다. 실제 명령어 옵션, Dockerfile 문법, 정확한 값은 공식 문서/`--help`/`man`을 직접 찾아 확인하세요.
4. 모든 실습은 **명령어 입력 + 출력 결과를 함께 캡처/기록**해야 최종 제출 시 증거로 인정됩니다. 진행하면서 바로바로 로그를 남겨두는 습관을 들이세요.
5. (서울캠퍼스) `sudo` 권한 제약이 있다면 Docker Desktop 대신 **OrbStack**을 사용하는 것이 전제되어 있습니다. 설치 전 이 부분부터 확인하세요.

---

## 0. 최종 제출물 총괄 체크리스트

전체 미션을 마치면 아래가 전부 하나의 저장소/기술 문서 안에서 확인 가능해야 합니다. 나머지 섹션(1~14)을 다 채우면 자연히 완성됩니다.

- [x] 기술 문서에 프로젝트 개요(미션 목표 요약) 포함
- [x] 기술 문서에 실행 환경(OS/쉘/터미널, Docker 버전, Git 버전) 명시
- [x] 기술 문서에 수행 항목 체크리스트 포함
- [x] 기술 문서에 검증 방법(어떤 명령으로 무엇을 확인했는지) + 결과 위치 링크 포함
- [x] 트러블슈팅 2건 이상 기록 (문제 → 원인 가설 → 확인 → 해결/대안)
- [x] 모든 명령/출력이 코드블록으로 정리되어 있는지 확인
- [ ] 민감정보(ID/PW, 토큰 등) 노출 여부 최종 점검

---

## 1. 학습 목표 자가 점검 (과제 목표)

미션의 목적은 "할 줄 아는 것"이 아니라 "설명할 수 있는 것"입니다. 아래 각 항목을 실습 후 자기 언어로 1~2문장씩 기술 문서에 정리해보세요.

**접근 힌트**
- 각 개념을 실습 전에 먼저 검색/문서로 학습 → 실습으로 검증 → 마지막에 "왜 그런지" 스스로 설명해보는 3단계로 진행하면 이해가 오래갑니다.
- 단순 암기가 아니라 "예시를 들어" 설명하라는 요구가 반복되므로, 실습 중 실제로 나온 예시(파일/컨테이너/포트 등)를 그대로 설명에 활용하세요.

> 정리: [`docs/study/concepts.md`](./docs/study/concepts.md)

- [x] 절대 경로와 상대 경로의 차이를 예시와 함께 설명할 수 있다
- [x] 파일 권한(r/w/x)의 의미와 755/644 같은 숫자 표기의 해석 규칙을 설명할 수 있다
- [x] 기존 Dockerfile을 기반으로 커스텀 이미지를 만드는 절차를 설명할 수 있다
- [x] 포트 매핑이 왜 필요한지 설명할 수 있다
- [x] Docker 볼륨(영속 데이터)의 개념을 설명할 수 있다
- [x] Git과 GitHub의 역할 차이(로컬 버전관리 vs 원격 협업 플랫폼)를 설명할 수 있다

---

## 2. 터미널 기초 조작 로그

**접근 힌트**
- "현재 위치 확인 → 목록 확인(숨김 파일 포함) → 이동 → 생성 → 복사 → 이동/이름변경 → 삭제 → 내용 확인 → 빈 파일 생성" 순서를 하나의 작업 디렉토리 안에서 이어서 실습하면 로그가 자연스러운 스토리가 됩니다.
- "숨김 파일 포함 목록"이라는 표현에 주목하세요 — 기본 옵션만으로는 안 보이는 파일이 있습니다.
- 각 명령 실행 직후 결과를 바로 캡처/복사해두는 습관이 나중에 편합니다.

> 실습 기록: [`docs/study/terminal-basics.md`](./docs/study/terminal-basics.md)

- [x] 현재 작업 위치 확인 명령 + 출력 기록
- [x] 숨김 파일 포함 목록 확인 명령 + 출력 기록
- [x] 디렉토리 이동 명령 + 출력 기록
- [x] 디렉토리/파일 생성 명령 + 출력 기록
- [x] 파일 복사 명령 + 출력 기록
- [x] 파일 이동/이름변경 명령 + 출력 기록
- [x] 파일/디렉토리 삭제 명령 + 출력 기록
- [x] 파일 내용 확인 명령 + 출력 기록
- [x] 빈 파일 생성 명령 + 출력 기록

---

## 3. 파일 권한 실습

**접근 힌트**
- r/w/x 각각이 파일과 디렉토리에서 의미가 다르다는 점(특히 디렉토리의 x)을 함께 정리하면 학습 목표(섹션 1) 답변이 쉬워집니다.
- "변경 전/후 비교"가 요구사항이므로, 권한 변경 전 상태를 먼저 확인·기록한 뒤 변경하고 다시 확인하는 before/after 쌍으로 로그를 남기세요.
- 숫자 표기(예: 755, 644)와 문자 표기(rwxr-xr-x 등)를 나란히 놓고 대응 규칙을 스스로 유도해보세요.

> 실습 기록: [`docs/study/permissions-guide.md`](./docs/study/permissions-guide.md)

- [x] 파일 1개에 대한 권한 확인(before) 기록
- [x] 파일 1개 권한 변경 명령 + 확인(after) 기록
- [x] 디렉토리 1개에 대한 권한 확인(before) 기록
- [x] 디렉토리 1개 권한 변경 명령 + 확인(after) 기록
- [x] 숫자 표기 ↔ r/w/x 대응 규칙을 정리한 설명 작성

---

## 4. Docker 설치 및 기본 점검

**접근 힌트**
- 먼저 본인 환경이 서울캠퍼스 정책(sudo 제약)의 대상인지 확인하고, 대상이라면 OrbStack 설치 경로로, 아니라면 일반 Docker 설치 경로로 진행하세요.
- "설치됨"과 "데몬이 실행 중임"은 별개입니다 — 버전 확인과 데몬 상태 확인을 각각 별도 명령으로 검증하세요.

> 실습 기록: [`docs/study/docker-install-check.md`](./docs/study/docker-install-check.md)

- [ ] (필요 시) OrbStack 설치 및 실행 확인 — N/A: 네이티브 Linux에 Docker Engine 설치, sudo 제약 없어 불필요
- [x] Docker 버전 확인 명령 + 출력 기록
- [x] Docker 데몬 동작 여부 확인 명령 + 출력 기록

---

## 5. Docker 기본 운영 명령 수행

**접근 힌트**
- "이미지"와 "컨테이너"는 다른 대상입니다 — 각각을 조회하는 명령이 다르다는 것을 실습으로 구분해보세요.
- "실행 중인 것만" 보는 조회와 "중지된 것까지 전부" 보는 조회의 차이를 의도적으로 비교해보세요.
- 리소스 확인 명령은 컨테이너가 실행 중일 때만 의미 있는 값이 나오니, 뭔가 하나 띄워둔 상태에서 확인하세요.

> 실습 기록: [`docs/study/docker-operations.md`](./docs/study/docker-operations.md)

- [x] 이미지 다운로드 명령 + 출력 기록
- [x] 이미지 목록 확인 명령 + 출력 기록
- [x] 컨테이너 실행 명령 + 출력 기록 (`docker run -d --name stats-demo ubuntu sleep infinity`)
- [x] 컨테이너 중지 명령 + 출력 기록
- [x] 실행 중 컨테이너 목록 확인 기록
- [x] 전체(중지 포함) 컨테이너 목록 확인 기록
- [x] 컨테이너 로그 확인 명령 + 출력 기록
- [x] 컨테이너 리소스 사용량 확인 명령 + 출력 기록

---

## 6. 컨테이너 실행 심화 실습

**접근 힌트**
- `hello-world`는 이미지를 받아 한 번 실행되고 끝나는 컨테이너입니다 — 왜 실행 후 바로 종료되는지 관찰해보세요.
- `ubuntu` 컨테이너는 기본적으로 아무 것도 안 하면 바로 종료됩니다. "내부에 진입해서 명령을 실행"하려면 컨테이너를 살아있게 유지하는 방법이 필요합니다 — 관련 옵션/명령을 검색해보세요.
- 컨테이너를 "실행한 상태로 붙는 것"과 "이미 떠 있는 컨테이너에 새 명령으로 들어가는 것"은 다른 메커니즘입니다. 두 가지를 각각 실습하고 차이를 직접 언어로 정리해보세요.

> 실습 기록: [`docs/study/container-deep-dive.md`](./docs/study/container-deep-dive.md)

- [x] `hello-world` 실행 성공 로그 기록
- [x] `ubuntu` 컨테이너 실행 + 내부 진입 성공 기록
- [x] 내부에서 간단 명령(목록/echo 등) 수행 결과 기록
- [x] 컨테이너를 살아있게 유지하는 방법 실습 및 기록
- [ ] "붙어서 실행 유지" vs "이미 떠있는 컨테이너에 진입"의 차이를 `attach`로 직접 관찰하여 정리 (`exit` → `Exited` 확인됨 [`image-5.png`], `Ctrl+P,Ctrl+Q` 분리 → `Up` 유지만 아직 — 절차는 study 문서 참고)

---

## 7. Dockerfile 기반 커스텀 이미지 제작 (웹 서버 컨테이너화)

**접근 힌트**
- 두 방식 중 하나를 선택합니다:
  - (A) 웹 서버 베이스 이미지 + 정적 콘텐츠/설정만 교체 (예: 정적 파일을 이미지 내부 특정 경로로 복사)
  - (B) 리눅스 베이스 이미지 + 기본 기능 추가 (패키지 설치, 사용자 추가, 환경변수, 헬스체크 등 중 일부)
- Dockerfile은 **반드시 직접 작성**해야 합니다 (요구사항). 기존 예제를 그대로 복붙하지 말고, 각 줄이 왜 필요한지 스스로 설명 가능하게 작성하세요.
- "커스텀 포인트 각각의 목적"을 문서화해야 하므로, Dockerfile을 작성하면서 각 명령어(FROM/COPY/RUN/ENV 등 중 사용한 것)마다 한 줄 메모를 남겨두면 나중에 정리가 쉽습니다.
- 빌드와 실행은 별개 단계입니다 — 빌드 성공 로그와 실행(컨테이너 기동) 성공 로그를 각각 남기세요.

> 빌드/실행 명령 가이드: [`docs/study/dockerfile-web-server-guide.md`](./docs/study/dockerfile-web-server-guide.md) (`docker images`에 이미 있는 `workstation-web:1.0`이 이 저장소 소스와 연결돼 있는지 먼저 확인할 것)

- [x] 베이스 방식 선택 (A 또는 B) 및 선택 이유 기록 — (A) nginx:1.30-alpine
- [x] Dockerfile 직접 작성 (레포에 포함) — `app/Dockerfile`
- [x] 웹 서버 소스코드/정적 콘텐츠 준비 (`app/` 또는 `src/` 등) — `app/site/`, `app/nginx/`
- [x] 커스텀 포인트별 목적 요약 작성
- [x] 이미지 빌드 명령 + 성공 로그 기록
- [x] 컨테이너 실행 명령 + 성공 로그 기록

---

## 8. 포트 매핑 검증

**접근 힌트**
- 컨테이너 내부 포트와 호스트(내 컴퓨터) 포트는 별개의 번호 체계입니다 — "왜 매핑이 필요한지"는 이 분리에서 나옵니다 (학습 목표 섹션 1과 연결).
- 요구사항은 **서로 다른 호스트 포트로 2회 이상** 접속 성공 증거입니다. 같은 이미지를 다른 호스트 포트로 두 번 실행하거나, 컨테이너 이름/포트를 바꿔 재실행해보세요.
- 브라우저 증거는 **주소창(포트 번호 포함)과 응답 화면이 함께** 보이도록 캡처해야 인정됩니다. `curl` 응답으로 대체할 경우도 명령+출력이 함께 보여야 합니다.

> 완료 기록: [`docs/study/dockerfile-web-server-guide.md`](./docs/study/dockerfile-web-server-guide.md) — 8080/8081 컨테이너 실행 + curl 200 OK + 브라우저 캡처(`docs/result/image-6.png`, `image-7.png`) 모두 확인됨.

- [x] 포트 매핑 개념(호스트 포트 ↔ 컨테이너 포트) 이해 및 설명 작성
- [x] 첫 번째 호스트 포트(8080)로 접속 성공 증거(주소창 포함) 확보
- [x] 두 번째 호스트 포트(8081)로 접속 성공 증거(주소창 포함) 확보

---

## 9. 바인드 마운트 & 볼륨 영속성 검증

**접근 힌트**
- 바인드 마운트는 "호스트의 특정 경로"를 컨테이너에 연결하는 방식이고, 볼륨은 "Docker가 관리하는 별도의 저장 공간"을 연결하는 방식입니다 — 이 차이를 실습으로 체감해보세요.
- 바인드 마운트 검증: 호스트에서 파일을 수정 → 컨테이너 안에서 변경이 즉시 반영되는지 확인 (반대 방향도 가능).
- 볼륨 영속성 검증 절차: ① 볼륨 생성 ② 컨테이너에 연결 ③ 컨테이너 안에서 데이터 기록 ④ **컨테이너를 삭제** ⑤ 같은 볼륨을 새 컨테이너에 연결해 데이터가 남아있는지 확인. "컨테이너 삭제 전/후 비교"가 핵심 증거입니다.

> 바인드 마운트 완료 기록: [`docs/study/bind-mount-guide.md`](./docs/study/bind-mount-guide.md)
> 볼륨 완료 기록: [`docs/study/volume-guide.md`](./docs/study/volume-guide.md)

- [x] 바인드 마운트로 컨테이너 실행 명령 기록
- [x] 호스트 측 변경 전 상태 기록
- [x] 호스트 측 변경 후 → 컨테이너 반영 확인 기록
- [x] Docker 볼륨 생성 명령 + 출력 기록
- [x] 볼륨을 컨테이너에 연결 + 데이터 기록
- [x] 컨테이너 삭제 명령 실행
- [x] 새 컨테이너에서 동일 볼륨 연결 → 데이터 유지 확인
- [x] 바인드 마운트 vs 볼륨 차이 설명 작성

---

## 10. Git 설정 및 GitHub/VSCode 연동

**접근 힌트**
- 사용자 정보(이름/이메일)와 기본 브랜치 설정은 커밋 이력에 남는 정보이므로 정확히 설정했는지 설정 조회 명령으로 재확인하세요.
- VSCode의 GitHub 로그인 및 저장소 연동은 GUI 기반이므로, 연동이 완료된 상태를 보여주는 스크린샷이 증거가 됩니다.
- 설정 조회 결과를 기술 문서에 기록할 때 **토큰/이메일 등 민감정보가 노출되지 않는지** 한 번 더 확인하세요 (섹션 11과 연결).

- [x] Git 사용자 이름/이메일 설정
- [x] 기본 브랜치 설정 (main)
- [x] 전체 Git 설정 조회 명령 + 출력 기록
- [x] VSCode에서 GitHub 로그인 완료
- [x] VSCode에서 저장소 연동 완료 + 증거(스크린샷) 첨부

---

## 11. 보안 및 개인정보 보호 점검

**접근 힌트**
- 제출 직전에 문서/스크린샷/로그를 한 번 전체 훑으며 토큰, 비밀번호, 개인키, 인증 코드가 그대로 노출된 부분이 없는지 검사하세요.
- 실수로 커밋된 민감정보가 있다면 단순 삭제 후 재커밋만으로는 Git 히스토리에 남을 수 있다는 점을 고려해, 필요 시 재발급까지 진행하세요.

- [ ] 최종 제출 전 문서/로그/스크린샷 전수 검사 (민감정보 마스킹)
- [ ] (해당 시) 노출된 민감정보 제거 및 재발급 절차 수행

---

## 12. 트러블슈팅 기록 (2건 이상 필수)

**접근 힌트**
- 실습 중 한 번에 안 되는 순간이 반드시 생깁니다 — 그 순간을 "문제 → 원인 가설 → 확인 방법 → 해결/대안" 4단 구조로 그때그때 메모해두면 나중에 정리하기 쉽습니다.
- 이미 해결된 문제를 억지로 만들기보다, Docker 설치, 포트 충돌, 권한 오류, 볼륨/마운트 경로 문제 등 실제로 부딪힌 것을 기록하는 편이 자연스럽습니다.

- [x] 트러블슈팅 사례 1 작성 (문제 / 원인 가설 / 확인 / 해결·대안) — `docker.sock` 권한 거부
- [x] 트러블슈팅 사례 2 작성 (문제 / 원인 가설 / 확인 / 해결·대안) — `containerd.io` 의존성 충돌

---

## 13. 제출 전 최종 점검 (제약 사항 재확인)

**접근 힌트**
- 이 섹션은 "다 했다고 생각했는데 놓친 것"을 잡아내는 최종 체크입니다. 제출 직전에 한 번 더 훑어보세요.

- [x] 모든 캡처/로그에 "명령어 입력"과 "출력 결과"가 함께 포함되어 있는지 확인
- [x] 브라우저 접속 증거에 주소창(포트 포함)이 함께 보이는지 확인 (`image-6.png`/`image-7.png`)
- [x] 개인 PC에 종속된 경로/설정이 있다면 대체 방법/주의사항을 문서에 기록했는지 확인 (README.md 2절에 안내 추가)
- [x] README(기술 문서)만 보고 제3자가 동일 절차를 재현할 수 있는지 자체 점검
- [x] 기술 문서 내 모든 명령/출력이 코드블록으로 정리되어 있는지 확인
- [x] GitHub 저장소 공개 설정(또는 규정된 권한) 확인 (`image-9.png` — Public 배지 확인)
- [x] 저장소 링크 하나로 모든 산출물에 접근 가능한지 확인 (git status clean, 전부 push 완료)

---

## 14. 보너스 과제 (선택)

**접근 힌트**
- 필수 과제를 모두 마친 뒤 여유가 있을 때 진행하세요. 각 항목은 필수 과제에서 다룬 개념(포트 매핑, 볼륨, Dockerfile)의 확장입니다.

> `app/compose.yaml`에 web(nginx)+probe(alpine, 헬스체크 폴링) 2개 서비스 구성 — 완료 기록: [`docs/study/dockerfile-web-server-guide.md`](./docs/study/dockerfile-web-server-guide.md) "Compose로도 동일하게 확인됨" 절

- [x] Docker Compose 기초: 파일 기본 구조 학습 + 단일 서비스 실행
- [x] Docker Compose 멀티 컨테이너: 웹 서버 + 보조 서비스 2개 이상 함께 실행 + 컨테이너 간 통신 확인 (`probe`가 `http://web/health` 서비스명으로 통신 성공)
- [x] Compose 운영 명령(기동/종료/상태/로그 확인) 실습 (`up`/`ps`/`logs`/`down` 전부 확인)
- [x] Dockerfile 또는 Compose에서 환경 변수로 포트/모드 변경해보기 (`WORKSTATION_HOST_PORT=9090 docker compose up -d web` → curl 200 확인)
- [ ] GitHub SSH 키 등록 및 SSH 방식 푸시 동작 확인 — `~/.ssh/id_ed25519` 키는 이미 존재하지만 GitHub에 등록되어 있지 않음(`ssh -T git@github.com` → Permission denied 확인). GitHub 웹에서 공개키 등록 후 재확인 필요

---

## 참고: 이 문서와 "기술 문서(제출용 README)"의 관계

이 파일은 **진행 체크리스트**입니다. 미션이 요구하는 "기술 문서"는 다음을 포함해야 하며, 위 체크리스트를 다 채운 뒤 그 결과(로그/스크린샷/설명)를 정리해 별도로(또는 이 문서를 확장해) 작성하면 됩니다.

- 프로젝트 개요
- 실행 환경
- 수행 항목 체크리스트
- 검증 방법 + 결과 링크
- 트러블슈팅 2건 이상
